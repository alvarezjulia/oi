{{!-- <h1>{{title}}</h1>
<p>Welcome to {{title}}</p> --}}
<div class="filter-button-wrapper">
  <button class="btn btn-primary" type="button" data-toggle="collapse" data-target="#collapseExample"
    aria-expanded="false" aria-controls="collapseExample">
    Filter locations
  </button>
</div>
<div class="collapse" id="collapseExample">
  <div class="filter-locations-wrapper">
    <form action="/" method="post">
      {{#each locationNames}}
      <div class="custom-control custom-checkbox" id="filter-location-box">
        <input name="filter-check{{this._id}}" type="checkbox" class="custom-control-input" id="customCheck{{this._id}}"
          value="{{this._id}}">
        <label class="custom-control-label" for="customCheck{{this._id}}">{{this.name}}</label>
      </div>
      {{/each}}
      <button type="submit">Filter!</button>
    </form>
  </div>
</div>

{{#each eventsObj}}

<div class="event-container" id="{{this._id}}">


  <div class="event-column-left">
    <form action="/auth/going/{{this._id}}" method="post">
      {{#if going}}
      <button type="submit" class="attending-event-button">Attending event</button>
      {{else}}
      <button type="submit">+ Going</button>
      {{/if}}
    </form>
  </div>

  <div class="event-column-center">
    <div class="location">{{this.location.name}}</div>
    <div class="event">{{event}}</div>
    <div class="info-wrapper">
      <div class="description">description: {{description}}</div>
    </div>
  </div>

  <div class="event-column-right"></div>

</div>
{{/each}}